# latex-terminal-setup → moved

This repo now lives on as the LaTeX half of
**[herdr-setup](https://github.com/DaebangStn/herdr-setup)**, which also installs
the herdr layer under it: the `hr*` helpers, the keybindings, the agent state
hooks, and the single skill and instructions file that every coding agent reads.

```sh
git clone https://github.com/DaebangStn/herdr-setup && cd herdr-setup
./install.sh
```

herdr-setup keeps this repo's full history; `SETUP.md` here is `LATEX.md` there.
Nothing further will be committed to this repo.
